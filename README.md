# Django React CRUD

Practice project for learning CRUD operations with Django, React, and Vite.

## Stack

- Django
- React
- Vite
- SQLite for local development

## Development

Run Django:

```powershell
python manage.py runserver
```

Run React/Vite:

```powershell
cd frontend
npm.cmd install
npm.cmd run dev
```
